// src/index.ts

// Pull in your styles so Vite includes them in the bundle:
import './index.css';

// (Later you’ll export your components here)
// For now, just a placeholder to satisfy TS:
export {};